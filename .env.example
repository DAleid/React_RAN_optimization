# ── Python backend (set on Render) ───────────────────────────────────────────
GROQ_API_KEY=gsk_your_key_here
LLM_PROVIDER=groq

# ── Next.js frontend (set on Vercel) ─────────────────────────────────────────
# Server-side rewrite target — points to your Render service URL
BACKEND_URL=https://your-render-service.onrender.com
